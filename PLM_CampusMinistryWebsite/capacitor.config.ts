import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PLM_CampusMiniistryWebsite',
  webDir: 'www'
};

export default config;
